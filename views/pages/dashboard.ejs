<%- include('../partials/header', { pageTitle: 'Dashboard' }) %>

<h1>Welcome to your Diary!</h1>

<div class="form-container">
  <h2>Add a New Entry</h2>
  <form method="POST" action="/entries">
    <div class="form-group">
      <label for="title">Title</label>
      <input type="text" id="title" name="title" required>
    </div>
    <div class="form-group">
      <label for="content">Content</label>
      <textarea id="content" name="content" rows="5" required></textarea>
    </div>
    <button type="submit">Save Entry</button>
  </form>
</div>


<h2>Your Existing Entries</h2>

<div class="entries-list">
  <% if (entries && entries.length > 0) { %>
    <% entries.forEach(entry => { %>
      <article class="entry">
        <h3><%= entry.title %></h3>
        <p class="entry-meta">
          Posted on: <%= new Date(entry.created_at).toLocaleDateString() %>
        </p>
        <p class="entry-content"><%= entry.content %></p>
        
        <div class="entry-actions">
          <a href="/entries/edit/<%= entry.id %>" class="btn-edit">Edit</a>
          
          <form action="/entries/delete/<%= entry.id %>" method="POST">
            <button type="submit" class="btn-delete">Delete</button>
          </form>
        </div>
      </article>
    <% }); %>
  <% } else { %>
    <p>You haven't written any entries yet. Why not add one now?</p>
  <% } %>
</div>

<%- include('../partials/footer') %>